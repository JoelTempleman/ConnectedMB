version: "3"

services:
  webserver:
    build:
      context: .
      dockerfile: dockerfiles/Dockerfile-webserver
    ports:
      - "80:80"
    volumes:
      - /var/www/html:/ConnectedMB/server/serverfiles/www
